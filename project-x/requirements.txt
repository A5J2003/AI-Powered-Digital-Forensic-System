# =====================================================================
# AI-Powered Digital Forensic System — Python Dependencies
# Install:  pip install -r requirements.txt
# =====================================================================

# ---------------------------------------------------------------------
# API SERVER
# ---------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
aiofiles==24.1.0

# ---------------------------------------------------------------------
# TEXT — NLP & AI detection
# ---------------------------------------------------------------------
transformers==4.46.3
sentencepiece==0.2.0
accelerate==1.1.1
captum==0.7.0
sentence-transformers

# ---------------------------------------------------------------------
# CORE DEEP LEARNING
# ---------------------------------------------------------------------
# TensorFlow (used for some model layers)
tensorflow==2.16.1
tf-keras

# PyTorch stack — check https://pytorch.org for CUDA-specific wheels
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2

# ---------------------------------------------------------------------
# AUDIO PROCESSING
# ---------------------------------------------------------------------
librosa==0.10.2.post1
soundfile==0.12.1
scipy==1.13.1
# openai-whisper — do NOT pin to 20240930, it fails to build on Python 3.12
# due to a pkg_resources bug in its setup.py. Let pip resolve the latest working version.
openai-whisper

# ---------------------------------------------------------------------
# VIDEO PROCESSING
# ---------------------------------------------------------------------
opencv-python==4.10.0.84
mtcnn==0.1.1
timm==1.0.11
grad-cam

# ---------------------------------------------------------------------
# IMAGE / VISUALIZATION
# ---------------------------------------------------------------------
Pillow==10.4.0
matplotlib==3.9.0
seaborn==0.13.2

# ---------------------------------------------------------------------
# DOCUMENT EXTRACTION (PDF / DOCX / TXT)
# ---------------------------------------------------------------------
PyPDF2==3.0.1
python-docx==1.1.2
# Note: 'textract' is intentionally excluded — it requires system-level
#       LibreOffice / antiword binaries that break on Windows.
#       PDF and DOCX are handled natively above.

# ---------------------------------------------------------------------
# OCR
# ---------------------------------------------------------------------
easyocr==1.7.2

# ---------------------------------------------------------------------
# REPORT GENERATION
# ---------------------------------------------------------------------
reportlab==4.2.5

# ---------------------------------------------------------------------
# MACHINE LEARNING & METRICS
# ---------------------------------------------------------------------
scikit-learn==1.5.1
numpy==1.26.4
pandas==2.2.2

# ---------------------------------------------------------------------
# MEDIA INFO (file metadata extraction)
# ---------------------------------------------------------------------
pymediainfo==6.1.0

# ---------------------------------------------------------------------
# UTILITIES
# ---------------------------------------------------------------------
tqdm==4.66.4
python-dotenv==1.0.1

# ---------------------------------------------------------------------
# OPTIONAL — interactive development
# ---------------------------------------------------------------------
# jupyter==1.0.0
